server:
  port: 9001
eureka:
    client:
        fetch-registry: true
        regiter-with-eureka: true
        service-url:
          default-zone: http://localhost:8761/eureka
        instance:
          hostname: localhost